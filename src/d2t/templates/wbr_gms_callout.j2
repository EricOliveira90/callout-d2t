{# WBR GMS YoY callout — single-paragraph business review format #}
{{ period_label }} GMS was {{ total_gms | compact_currency(signed=false) }}, {{ yoy_pct | signed_pct }} YoY ({{ yoy_delta | compact_currency }}){% if has_goals %}, and {{ goal_pct | signed_pct }} vs goal{% endif %}.
{%- if positive_categories %} Main {% if has_goals %}vs-goal{% else %}YoY{% endif %} GMS positive contribution came from: {% for cat in positive_categories %}{{ "abcdefghijklmnopqrstuvwxyz"[loop.index0] }}) {{ cat.product_group }} ({% if has_goals %}{{ cat.goal_pct | signed_pct }} vs goal, {{ cat.goal_delta | compact_currency }}{% else %}{{ cat.yoy_pct | signed_pct }}, {{ cat.yoy_delta | compact_currency }} YoY{% endif %})
{%- if cat.top_seller_name %}, driven by {{ cat.top_seller_name }} ({{ cat.top_seller_program }}, {{ cat.top_seller_yoy_delta | compact_currency }}){% endif %}
{%- if not loop.last %}; {% endif %}
{%- endfor %}.{% endif %}
{%- if negative_categories %} On the negative side, {% for cat in negative_categories %}{{ "abcdefghijklmnopqrstuvwxyz"[positive_categories | length + loop.index0] }}) {{ cat.product_group }} ({% if has_goals %}{{ cat.goal_pct | signed_pct }} vs goal, {{ cat.goal_delta | compact_currency }}{% else %}{{ cat.yoy_pct | signed_pct }}, {{ cat.yoy_delta | compact_currency }} YoY{% endif %}) was {% if loop.first %}the main offender{% else %}also an offender{% endif %}
{%- if cat.top_seller_name %}, driven negatively by {{ cat.top_seller_name }} ({{ cat.top_seller_program }}, {{ cat.top_seller_yoy_delta | compact_currency }}){% endif %}
{%- if not loop.last %}; {% endif %}
{%- endfor %}.{% endif %}
